name: vulcan(example-2-wip-gcov-exclusion-list)

on:
  workflow_dispatch:

jobs:
  vulcan:
    runs-on: self-hosted

    steps:
      - name: Test vulcan action
        uses: Suresoft-GLaDOS/vulcan-action@wip-gcov-exclusion-list
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          path: vulcan_target